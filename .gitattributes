# 默认让 Git 自动检测文本文件，并在签出/提交时将其规范化为 LF
* text=auto eol=lf

# 明确指定特定文档文件和脚本使用 LF
*.md text eol=lf
*.txt text eol=lf
*.sh text eol=lf
*.json text eol=lf

# 确保二进制文件（如图片）不被错误转换
*.png binary
*.jpg binary
*.gif binary